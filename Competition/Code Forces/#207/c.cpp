#include <bits/stdc++.h>
using namespace std;
int height,weight,n,ans;
struct typedef node{
    int l,r;
    int beat;
}knight[N];
void build(int n,int r.int l){
}
int main()
{
	int i,a;
	while(scanf("%d%d",&n,&m)!=EOF){
		build(1,1,n);
		for(i=1;i<=m;i++){
            scanf("%d%d%d",&a,&b,&x);
            update(1,3,x);
		}
	}
	return 0;
}

